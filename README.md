# vanilla/garden-middleware

Garden middleware is a set of utilities for creating middleware.
Notably middleware are created with generators so you don't have them cluttering your
execution stack traces.
